{
  "full_repo_runtime_dependency": false,
  "frozen_deployment": {
    "samples": 20,
    "selection_horizon": 16,
    "minimum_chunk_size": 2,
    "continuous_entropy": "per-arm seven-joint Gaussian differential entropy",
    "gripper_entropy": "per-arm Bernoulli entropy at normalized threshold zero",
    "chunk_rule": "upstream cumulative-mean entropy elbow",
    "sample_rule": "upstream nearest sample to multi-sample mean"
  }
}
