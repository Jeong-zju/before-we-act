$schema: https://json-schema.org/draft/2020-12/schema
$id: wam.robofactory.wam_flow.evidence_board/1
type: object
additionalProperties: false
required:
  - format_version
  - round_id
  - candidates
  - hard_gates
  - decision
properties:
  format_version:
    const: wam.robofactory.wam_flow.evidence_board/1
  round_id:
    type: string
  candidates:
    type: object
  hard_gates:
    type: array
    items:
      type: string
  decision:
    type: object
